let Section = require("../../../components/Section");
let Layout = require("../layout");

module.exports = () => (
  <Layout title={["Signals plugin", <small>(experimental)</small>]}>
    <Section title={["Signals plugin", <small>(experimental)</small>]} />
  </Layout>
);
